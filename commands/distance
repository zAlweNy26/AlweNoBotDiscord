const mine = require('../functions.js')
const Discord = require("discord.js")
const request = require("request")

//process.env.bingAPI
//https://docs.microsoft.com/en-us/bingmaps/rest-services/routes/calculate-a-route

module.exports = {
  	name: 'distance',
  	description: '*Mostra informazioni sulla distanza tra una città e un\'altra*',
  	aliases: ['dt'],
  	category: 'Misc',
  	usage: '[partenza] [destinazione]',
  	userPermissions: ['SEND_MESSAGES', 'EMBED_LINKS'],
    guildOnly: true,
    ownerOnly: false,
    examples: ['dt Trapani|Torino', 'distance Milano|Roma'],
    args: -1,
    execute(message, args) {

    },
}
